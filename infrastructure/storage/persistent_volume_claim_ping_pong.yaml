apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: exercises
  name: ping-pong-pvc
spec:
  storageClassName: ping-pong-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi