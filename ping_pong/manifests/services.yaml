apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: ping-pong-service
spec:
  type: ClusterIP
  selector:
    app: ping-pong
  ports:
    - protocol: TCP
      port: 3456
      targetPort: 8002